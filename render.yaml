services:
  # 1. The Express Backend
  - type: web
    name: planora-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
